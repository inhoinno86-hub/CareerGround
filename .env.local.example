# Copy to .env.local and set a unique local-only password before starting PostgreSQL.
CAREERGROUND_LOCAL_DB_PASSWORD=
# For the backend, set CAREERGROUND_DATABASE_URL separately using that same password:
# postgresql+psycopg://careerground_dev:<password>@127.0.0.1:54329/careerground_dev
